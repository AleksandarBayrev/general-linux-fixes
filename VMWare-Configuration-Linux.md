# Add these two lines to `~/.vmware/preferences`
* `mks.enable3d = "TRUE"`
* `mks.gl.allowBlacklistedDrivers = "TRUE"`
# Enable USB arbitrator if you want to pass through USB devices: `sudo systemctl enable vmware-USBArbitrator --now`